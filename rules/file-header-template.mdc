---
alwaysApply: true
---

**在所有需要提及默认作者和版权信息的地方本规则为最高优先级**

# 文件头注释模板规范

## 规则1.1 所有源文件和头文件必须包含标准文件头注释

所有 `.c` 和 `.h` 文件必须在文件开头包含标准格式的文件头注释。

## 默认信息

### 默认作者信息
- **作者名称**：Scienheim
- **作者邮箱**：junjie.zong@tritrondf.com
- **版本号**：V0.0.1（初始版本）
- **日期格式**：YYYY-MM-DD （获取实时时间填写）

### 默认版权信息
- **版权归属**：Tritron Technologies Co., Ltd.
- **版权年份**：使用创建年份和当前年份范围（如：2025-2026）
- **版权声明格式**：`Copyright (c) [公司名称] [起始年份]-[当前年份]. All rights reserved.`

## 文件头注释模板

```c
/******************************************************************************
Copyright (c) [公司名称] [起始年份]-[当前年份]. All rights reserved.

File name: [文件名]
Author: [作者姓名]
Version: [版本号]
Date: [创建日期 YYYY-MM-DD]
Description: [用于详细说明此程序文件完成的主要功能，与其他模块或函数的接口]
Others: [其它内容的说明，如依赖关系、注意事项等]
History: 
1. Date: [创建日期]
   Author: [作者姓名]
   Modification: Initial version created
2. Date: [修改日期]
   Author: [修改者]
   Modification: [修改内容简述]
******************************************************************************/
```

### 示例

```c
/******************************************************************************
Copyright (c) Tritron Technologies Co., Ltd. 2025-2026. All rights reserved.

File name: bsp_uart.c
Author: Zhang San
Version: V0.0.1
Date: 2025-01-23
Description: UART hardware initialization module, supports multi-UART configuration and interrupt setup
Others: Depends on the GD32H7xx Standard Peripheral Library
History: 
1. Date: 2025-01-23
   Author: Zhang San
   Modification: Initial version created
2. Date: 2025-01-26
   Author: Zhang San
   Modification: Added initialization support for UART3 and UART4
******************************************************************************/
```

## 规则1.2 必需字段

文件头注释必须包含以下字段：
- **Copyright**：版权声明（必需）
- **File name**：文件名（必需）
- **Author**：作者姓名（必需）
- **Version**：版本号（必需）
- **Date**：创建日期（必需）
- **Description**：功能描述（必需，使用英文）
- **Others**：其他说明（可选）
- **History**：修改历史（必需，至少包含初始创建记录）

## 规则1.3 History 更新原则（禁止机械逐次堆砌）

修改文件时**必须**使 History 能反映真实变更，但**不要求**每次保存或每次微小改动都**新增一条**带编号的记录。短时间内反复改同一文件时，应**合并记录**，避免 History 冗长难读。

### 1.3.1 合并记录（默认做法）

满足下列情形时，**优先合并为一条** History（更新该条目的 `Date` 为**最后完成该批工作的日期**、`Modification` 概括本批全部要点），而不是连续追加 `3.`、`4.`、`5.`：

- 同一作者、围绕**同一目标**在连续工作时段内（例如同一工作日、同一次需求/缺陷修复、同一会话内多次迭代）的多次修改；
- 同属一次代码评审往返中的琐碎修正（命名、格式、边界条件微调等）。

`Modification` 内可用分号分隔多项简述，或用一句**总体概括**（如：`Refine USART init and error handling for review feedback`）。

### 1.3.2 何时新增一条（单独编号）

在下列情况下**再新增**一条 History 条目（在合并记录之后顺延编号）：

- 对外可见的**里程碑**：如版本号变更、公开 API/行为语义变更、重大功能交付；
- 文件**职责或架构**发生明显变化，继续写在上一笔「合并记录」里会失真或过长；
- 与上一笔记录之间已隔较长时间或不同任务，且变更不宜与上一笔合并叙述。

每条记录仍须包含：`Date`、`Author`、`Modification`。

### 1.3.3 与版权、版本、描述的关系

- **Copyright 年份**仍按规则 1.4 在每次实质修改时更新；
- **Version**、**Description** 在接口或功能含义变化时同步更新，不因「少写 History 条数」而省略。

### 1.3.4 底线

- History **至少保留初始创建**记录，不得删除；
- **禁止**为刷历史而写无信息量的条目（如仅写 `Update`、重复同义句）；**禁止**用大量单行碎片替代一次可合并的说明。

## 规则1.4 版权年份范围应自动更新

版权声明中的年份范围应反映文件的创建年份和最新修改年份。如果文件在当年创建且未跨年，可以只写一个年份。

## 建议1.1 版本号遵循语义化版本规范

版本号建议遵循语义化版本规范：`V主版本号.次版本号.修订号`（如：V1.0.0、V1.1.0、V2.0.0）
- 主版本号：不兼容的API修改
- 次版本号：向下兼容的功能性新增
- 修订号：向下兼容的问题修正

## 建议1.2 History 记录范围与工具使用约定

在遵守规则 1.3 **合并优先**的前提下，History 侧重：**初始创建**（必需）、重大功能变更、API 与行为变更、重要缺陷修复、架构或设计调整。纯格式、错别字、与行为无关的注释润色等，若已包含在某次合并记录的概括句中即可，**不必**单独占一条。

使用 AI/自动化辅助时：对**同一文件在同一任务内**的连续修改，应**改写或合并**已有 History 条目，而不是默认每次 diff 都追加新编号。

**注意**：在实际使用时，请将模板中的占位符（如 `[公司名称]`、`[作者姓名]`等）替换为实际的项目信息