---
alwaysApply: true
---

**该规则为基本规则，当出现和其他规则冲突且未声明优先级的情况时优先采取本规则**

# C语言编程规范

## 总体原则

代码需要在保证**功能正确**的前提下，满足**可读、可维护、安全、可靠、可测试、高效、可移植**的特征要求。

**规则**：编程时必须遵守的约定  
**建议**：编程时必须加以考虑的约定

**例外**：在不违背总体原则，经过充分考虑，有充足的理由的前提下，可以适当违背规范中约定。例外破坏了代码的一致性，请尽量避免。"规则"的例外应该是极少的。

**修改外部开源代码、第三方代码时，应该遵守开源代码、第三方代码已有规范，保持风格统一。**

### 与其他 Cursor 规则的关系（避免冲突）

- **文件头**：版权年份、默认作者、字段名、`History` 合并与更新方式等，以仓库内 **`file-header-template.mdc`** 为准；本规范 **规则 3.1** 仅规定「必须覆盖的信息类别」，不与之抢格式细节。
- **目录与分层命名**（如 `app_` / `bsp_` 前缀、模块边界）：以 **`directory-structure-guide.mdc`** 为准；本规范 **规则 1.1** 的驼峰规则作用于标识符主体拼写，**与模块前缀并存**（例如对外 API 在满足分层前缀约定下仍使用大驼峰函数名）。
- **`#include` 与分层 / 可见性**（各层能否包含厂商库、BSP 对外头等）：以 **`directory-structure-guide.mdc`** 为准；本规范 **第四章**仅约定 `.c` / `.h` 中 **`#include` 的位置、内容、顺序、形式与最小依赖**等书写习惯。

---

# 一、命名规范

## 规则1.1 标识符命名使用驼峰风格

| 类型 | 命名风格 |
| --- | --- |
| 函数，结构体类型，枚举类型，联合体类型 | 大驼峰 |
| 变量，函数参数，宏参数，结构体中字段，联合体中成员 | 小驼峰 |
| 宏，常量，枚举值，goto 标签 | 全大写，下划线分割 |

**注意**：**常量**是指全局作用域下，const 修饰的**基本数据类型**、**枚举**、**字符串类型**的变量，不包括**数组**、**结构体**和**联合体**。

### 建议1.1 作用域越大，命名应越精确

全局作用域下的标识符命名需要考虑冲突问题。对于全局函数、全局变量、宏、类型名、枚举名的命名，应当精确描述并全局唯一。必要时可以增加模块前缀，模块前缀与命名主体之间，按驼峰方式连接。

### 建议1.2 文件命名只许使用小写字符、数字和下划线

文件名应尽量**简短、准确、无二义性**。

### 建议1.3 函数的命名遵循阅读习惯

动作类函数名可以使用动宾结构；判断型函数可以用形容词或加 is；数据型函数可直接命名或加 Get 前缀。

### 规则1.2 尽可能少的使用全局变量，使用时应增加 'g_' 前缀

函数内静态变量命名不需要加特殊前缀。常量本质也是全局变量，但如果命名风格是全大写，下划线连接的格式，则不适用当前规则。

### 建议1.4 局部变量应该简短，且能够表达相关含义

tmp 可以用来称呼任意类型的临时变量。过短的变量命名应慎用，部分情况下，单字符变量被允许使用，如用于循环语句中的计数器变量或简单的数学计算函数中的变量。

### 建议1.5 类型命名采用大驼峰命名风格

通过 typedef 对结构体、联合体、枚举**起别名**时，尽量使用匿名类型。若需要指针自嵌套，可以增加 'tag' 前缀或下划线后缀。

### 建议1.6 函数式宏中的临时变量使用后置下划线

**非必要不使用函数式宏。**函数式宏中定义局部变量时使用后置下划线。

---

# 二、排版格式

### 规则2.1 使用空格进行缩进，每次缩进4个空格

### 建议2.1 行宽不超过 120 个字符

如下场景不宜换行，可以例外：换行会导致内容截断，无法被方便查找(grep)的字符串；#include / #error 语句可以超出行宽要求，但需尽量避免。

### 规则2.2 大括号使用规范

函数，左大括号和右大括号单独占一行；for / while、if / else if / else、switch、struct、union、enum，左大括号与关键字同行，右大括号独占一行。

### 规则2.3 函数声明、定义的返回类型和函数名在同一行；函数参数列表换行时应合理对齐

参数列表的左圆括号总是和函数名在同一行；右圆括号总是跟随最后一个参数。

### 规则2.4 函数调用参数列表换行时保持参数进行合理对齐

左圆括号总是跟函数名，右圆括号总是跟最后一个参数。如果函数调用的参数存在内在关联性，按照可理解性优先于格式排版要求，对参数进行合理分组换行。

### 规则2.5 所有条件语句和循环语句必须使用大括号

### 规则2.6 禁止 if/else/else if 写在同一行

### 规则2.7 switch 语句的 case/default 要缩进一层；不使用大括号；禁止无 break 的贯穿

每个 `case` / `default` 分支必须以 **`break`**、`return` 等**明确结束当前分支**，或落在**团队认可的贯穿标注**之后进入下一分支。

**允许的刻意贯穿（例外）**：在紧邻下一 `case`（或 `default`）之前，使用**可检索、可统一**的贯穿声明，例如 C23 属性 `[[fallthrough]]`、GNU/Clang `__attribute__((fallthrough))`，或项目文档明确列出的等价写法（如特定注释形式）。**禁止**「无 `break` 且无上述标注」的静默贯穿。

### 建议2.2 表达式换行要保持换行的一致性，操作符放行末

较长的表达式，不满足行宽要求时，在较低优先级操作符或连接符后面截断，操作符或连接符放在行末。表达式换行后，注意保持合理对齐，或者4空格缩进。

### 规则2.8 多个变量定义和赋值语句不允许写在一行

例外：对于多个相关性强的变量定义，且无需初始化时，可以定义在一行。

### 规则2.9 初始化换行时要有缩进，或进行合理对齐

结构体或数组初始化时，如果换行应保持4空格缩进。结构体和联合体在按成员初始化时，每个成员初始化单独一行。

### 建议2.3 指针类型"*"跟随变量名或者类型，保持一致性

选择"*"跟随类型风格时，避免一行同时声明带指针的多个变量。注意，任何时候 "*" 不要紧跟 const 或 restrict 关键字。

### 规则2.10 编译预处理的"#"默认放在行首，嵌套编译预处理语句时，"#"可以进行缩进

### 规则2.11 水平空格应该突出关键字和重要信息，每行代码尾部不要加空格

- if, switch, case, do, while, for 等关键字之后**不加**空格
- 小括号内部的两侧，不要加空格
- 二元操作符（= + - < > * / % | & ^ <= >= == !=）左右两侧加空格
- 一元操作符（& * + - ~ !）之后不要加空格
- 三目操作符（? :）符号两侧均需要空格
- 前置和后置的自增、自减（++ --）和变量之间不加空格
- 结构体成员操作符（. ->）前后不加空格
- 大括号内部两侧有无空格，左右必须保持一致
- 逗号、分号、冒号（不含三目操作符和表示位域的冒号）紧跟前面内容无空格，其后需要空格

对于大括号内部两侧的空格，**建议**：一般的，大括号内部两侧建议加空格；对于空的，或单个标识符，或单个字面常量，空格不是必须；连续嵌套的多重括号之间，空格不是必须。

### 建议2.4 合理安排空行，保持代码紧凑

根据上下内容的相关程度，合理安排空行；函数内部、类型定义内部、宏内部、初始化表达式内部，不使用连续空行；不使用连续 **3** 个空行，或更多，最多两行空行；大括号内的代码块首行之前和末行之后不要加空行。

---

# 三、注释

注释是为了帮助阅读者快速读懂代码，从读者的角度出发，**按需注释**。注释内容要简洁、明了、无二义性，信息全面且不冗余。注释解释代码难以表达的意图，**不要重复代码信息**。**修改代码时，也要保证其相关注释的一致性**。使用英文进行注释。

在 C 代码中，使用 /* */和 // 都是可以的。按注释的目的和位置，注释可分为不同的类型，如文件头注释、函数头注释、代码注释等等；**同一类型的注释**应该**保持统一的风格**。

### 规则3.1 文件头部应进行注释，注释必须列出：版权说明、版本号、生成日期、作者姓名、内容、功能说明、与其它文件的关系、修改日志等

**格式、字段顺序、版权年份写法及 `History` 更新策略**以 **`file-header-template.mdc`** 为单一事实来源；此处不要求重复展开。版权声明须体现**创建年至当前修改年**（可跨年写范围），与模板规则保持一致。

### 规则3.2 禁止空有格式的函数头注释

并不是所有的函数都需要函数头注释；函数原型无法表达的信息，加函数头注释辅助说明。函数头注释统一放在函数声明或定义上方。函数尽量通过函数名自注释，**按需**写函数头注释。**不要写无用、信息冗余的函数头**。

函数头注释内容**可选**，但不限于：功能说明、返回值，性能约束、用法、内存约定、算法实现、可重入的要求等等。

### 规则3.3 代码注释放于对应代码的上方或右边

### 规则3.4 注释符与注释内容间要有1空格；右置注释与前面代码至少1空格

代码上方的注释，应该保持对应代码一样的缩进。右置格式在适当的时候，上下对齐会更美观。

### 规则3.5 最终版本的代码，不用的代码段直接删除，不要注释掉

这里说的注释掉代码，包括用 /* */ 和 //，还包括 #if 0， #ifdef NEVER_DEFINED 等等。

### 建议3.1 正式交付的代码不能包含 TODO/TBD/FIXME 注释

---

# 四、头文件

头文件中适合放置接口的声明，不适合放置实现（内联函数除外）。

### 建议4.1 每一个.c文件都应该有相应的.h文件，用于声明需要对外公开的接口

例外：程序的入口（如main函数所在的文件），单元测试代码，动态库代码。

**内部**使用的函数声明，宏、枚举、结构体等定义**不应放在头文件中**。

头文件包含是一种依赖关系，头文件应向稳定的方向包含。依赖的方向应该是：产品依赖于平台，平台依赖于标准库。更好的方式是每个模块都依赖于接口，这样任何一个模块的内部实现更改都不需要重新编译另外一个模块。

### 规则4.1 禁止头文件循环依赖

### 规则4.2 头文件必须编写#define保护，防止重复包含

为防止头文件被多重包含，所有头文件都应当使用 #define 作为包含保护。保护符使用唯一名称；建议考虑项目源代码树顶层以下的文件路径。不要在受保护部分的前后放置代码或者注释，文件头注释除外。

### 规则4.3 禁止通过声明的方式引用外部函数接口、变量

应通过包含头文件的方式使用其他.c提供的接口。

例外：针对某一内部函数进行单元测试时，可以通过 extern 声明来引用被测函数；当需要对某一函数进行打桩、打补丁处理时，允许 extern 声明该函数。打桩打补丁操作非必要不使用。

### 规则4.4 禁止在 extern "C" 中包含头文件

### 规则4.5 `#include` 书写位置（`.c` / `.h` 通用）

所有 `#include` 须集中在**文件头注释之后**；对于 `.h`，还须位于**包含保护宏开启之后**（见 **规则4.2**）、**任何其他顶层次代码**（宏、类型、函数声明或实现等）之前。**禁止**在函数体内或文件末尾随意追加 `#include`。团队书面约定的极少数移植场景例外时，须附简短注释说明理由。

### 建议4.2 实现文件 `.c`：`#include` 内容与顺序

**内容**：除配对头外，仅包含本 `.c` **直接使用**的类型、宏、函数声明等符号所在头；**不依赖**「其他头碰巧已间接包含」。分层上允许包含哪些头以 **`directory-structure-guide.mdc`** 为准。

**顺序**：**首条** `#include` 宜为与本 `.c` **同名**的对外 `.h`（若存在），用于验证该头自洽。其后建议分组，**组与组之间空一行**，组顺序宜为：**其他本项目头** → **第三方 / 厂商头**（在分层允许时）→ **C 标准库**。**组内**排序与仓库统一：常见为按路径**字母序**，或按依赖从**更基础到更上层**；全仓库择一贯彻。

### 建议4.3 头文件 `.h`：`#include` 内容与顺序

**内容**：仅包含使本头文件中**已出现的对外声明 / 定义**在**单独被包含**时能够**合法编译**所**直接**需要的头。能通过**前置声明**（如 `struct Foo;`、配合指针使用的 `typedef struct Foo Foo;` 等）避免暴露完整类型的，**优先前置声明**，以减少传递依赖。**禁止**在对外 API 头中为「调用者省事」而包含与**本头公开 API 无关**的头（除非文档或注释**明确**约定须组合包含）。

**顺序**：`#include` 位于包含保护宏开启之后、**其余声明 / 定义**之前。分组与空行规则宜与 `.c` **对齐**：**本模块所依赖的其他本项目头** → **允许的第三方 / 厂商头** → **C 标准库**；组间空一行，组内排序策略与 `.c` 一致。`.h` **无**「配对头置顶」；若需包含本模块**其它**私有或内部头，放入「其他本项目头」组，并遵守 **`directory-structure-guide.mdc`** 中对外头与域内头的可见性约定。

### 建议4.4 `#include` 使用尖括号与双引号的形式

`#include <...>` 用于 C 标准库及工程约定按**系统式**包含路径解析的头。`#include "..."` 用于本项目头及工程约定按**项目相对**路径解析的头。具体以当前工具链及 `eide.yml` 等配置中的 `-I` 约定为准；**同一类头在仓库内用法保持一致**。

### 建议4.5 最小包含与对外头的自包含

与 **规则9.2**（禁止包含冗余头文件）一致：禁止仅为「曾经能编过」而保留冗余 `#include`；接口变更时应在**直接使用者**处增删包含。**对外 API 头**宜**自包含**：在遵守分层与最小依赖前提下，典型调用方**仅** `#include` 该头即可获得其公开接口所需的类型与宏；若必须组合包含其它头，须在注释或文档中**显式**说明。

**例外**：第三方 / 厂商冻结源码若原有包含风格不同，维持其既有规范（与文首「修改外部开源代码」一致）。

---

# 五、函数

函数应该简洁、短小。一个函数只完成一件事情。

### 规则5.1 避免函数过长，函数不超过50行（非空非注释）

例外：考虑代码的聚合性与功能的全面性，某些函数可能会超过50行，但前提是不影响代码的可读性与简洁。这些例外的函数应该是极少的，例如特定算法处理。在允许的情况下，将长函数拆分为更加简短并易于管理的若干函数。

### 规则5.2 避免函数的代码块嵌套过深，不要超过4层

函数的代码块嵌套深度指的是函数中的代码控制块（例如：if、for、while、switch等）之间互相包含的深度。使用卫语句可以有效的减少 if 相关的嵌套层次。

例外：考虑代码的聚合性与功能的全面性，某些函数嵌套可能会超过4层，但前提是不影响代码的可读性与简洁。这些例外的函数应该是极少的。

### 建议5.1 对函数的错误返回码要全面处理

一个函数能够提供一些指示错误发生的方法。不管什么时候函数提供了这样的机制，调用程序应该在函数返回时立刻检查错误指示。

注意，当函数返回值被大量的显式(void)忽略掉时，应当考虑函数返回值的设计是否合理。如果所有调用者都不关注函数返回值时，请将函数设计成void型。

### 建议5.2 设计函数时，优先使用返回值而不是输出参数

### 建议5.3 使用强类型参数，避免使用void*

例外：某些通用泛型接口，需要传入不同类型指针的，可以用 void * 入参。

### 建议5.4 模块内部函数参数的合法性检查，由调用者负责

### 建议5.5 函数的指针参数如果不是用于修改所指向的对象就应该声明为指向const的指针

注意：指针参数要不要加 const 取决于函数设计，而不是看函数实体内有没有发生"修改对象"的动作。

### 建议5.6 函数的参数个数不超过5个

### 建议5.7 内联函数不超过10行（非空非注释）

### 规则5.3 被多个源文件调用的内联函数要放在头文件中定义

**gnu89** 在内联函数实现上跟**C99**标准有差异，兼容做法是将函数声明为 **static inline**。

---

# 六、宏

**非必要不使用函数式宏**

函数式宏是指形如函数的宏，其包含若干条语句来实现某一特定功能。

### 建议6.1 尽量使用函数代替函数式宏

定义函数式宏前，应考虑能否用函数替代。对于可替代场景，建议用函数替代宏。对于性能敏感的代码，可以考虑用内联函数代替函数式宏。函数和内联函数不能完全替代函数式宏，函数式宏在某些场景更适合，比如在日志记录场景下，使用带可变参和默认参数的函数式宏更方便。

### 规则6.1 定义宏时，宏参数要使用完备的括号

避免滥用括号。下列情况需要注意：宏参数参与 '#', '##' 操作时，不要加括号；宏参数参与字符串拼接时，不要加括号；宏参数作为独立部分，在赋值（包括+=, -=等）操作的某一边时，无需括号；宏参数作为独立部分，在逗号表达式，函数或宏调用列表中，无需括号。

```c
#define SUM(a, b) a + b // Bad
#define SUM(a, b) ((a) + (b)) // Good
```

### 规则6.2 包含多条语句的函数式宏的实现语句必须放在 do-while(0) 中

例外：包含 break, continue 语句的宏可以例外；宏中包含不完整语句时，可以例外；非多条语句，或单个 if/for/while/switch 语句，可以例外。

### 规则6.3 不允许把带副作用的表达式作为参数传递给函数式宏

### 建议6.2 函数式宏定义中的控制流

**禁止**在函数式宏中使用 `goto`：与规则 8.4 一致，宏展开后的代码仍属本规范约束，**不得以宏为壳绕过** `goto` 禁令。

对 `return`、`continue`、`break` 仍须**慎用**；非必要不在宏内改变调用方控制流。若宏的语义必须包含上述关键字之一，**宏命名须能体现对应关键字**，以免误用。

### 建议6.3 函数式宏不超过10行(非空非注释)

---

# 七、变量

尽量不用或少用全局变量。如不可避免，对全局变量的读写应集中封装。

### 规则7.1 模块间，禁止使用全局变量作接口

### 规则7.2 严禁使用未经初始化的变量

这里的变量，指的是局部动态变量，并且还包括内存堆上申请的内存块。因为他们的初始值都是不可预料的，所以禁止未经有效初始化就直接读取其值。如果有不同分支，要确保所有分支都得到初始化后才能使用。

### 规则7.3 禁止无效、冗余的变量初始化

对变量或内存块进行**正确**、**必要**的初始化。按需定义，在**靠近变量使用的地方定义变量**。例外：遵从"安全规范"要求，指针变量、表示资源描述符的变量、BOOL变量不作要求。

### 规则7.4 不允许使用魔鬼数字

所谓魔鬼数字即**看不懂、难以理解**的数字。解决途径：对于单点使用的数字，可以增加注释说明；对于多处使用的数字，必须定义宏或const 变量，并通过符号命名自注释。

禁止出现下列情况：没有通过符号来解释数字含义，如 `#define ZERO 0`；符号命名限制了其取值，如 `#define XX_TIMER_INTERVAL_300MS 300`。

---

# 八、编程实践

### 建议8.1 表达式的比较，应当遵循左侧倾向于变化（变量）、右侧倾向于不变（常量）的原则

也有特殊情况，如：`if(MIN < v && v < MAX)` 用来描述区间时，前半段是常量在左的（`if` 与 `(` 之间无空格，同规则 2.11）。

### 规则8.1 含有变量自增或自减运算的表达式中禁止再次引用该变量

**简而言之，i++;独占一行**

### 规则8.2 用括号明确表达式的操作顺序，避免过分依赖默认优先级

需要注意的是过多的括号会分散代码使其降低可读性，应适度使用。当表达式包含不常用，优先级易混淆的操作符时，推荐使用括号，比如位操作符。

### 规则8.3 switch 语句必须有 default 分支

**禁止**省略 `default`，包括 `switch` 条件为枚举类型且 `case` 已**字面覆盖**所有枚举取值的情形。`default` 用于处理未知值、防御性分支或统一收尾；若逻辑上不可达，须在 `default` 内采用团队约定方式显式处理（例如断言、错误上报或 `break`），**不得**以「已全覆盖」为由删除 `default`。

### 规则8.4 禁止使用 goto 语句

源码中（含由宏展开产生的语句）**不得**使用 `goto`。函数式宏内的 `goto` 同样禁止，见建议 6.2。

### 建议8.2 尽量减少没有必要的数据类型默认转换与强制转换

---

# 九、模块化开发

### 规则9.1 模块化的头文件不允许声明多余的内容

头文件只能声明**外部需要使用**的 宏定义，结构体/联合体/枚举类型定义，全局变量的extern声明，函数声明。**禁止**在头文件里定义全局变量。**禁止**在头文件里面做函数实现（前文提及到的特殊情况下的内联函数除外）。

### 规则9.2 模块化的源文件遵循的编写规范

**不对外部开放**的 宏定义，结构体/联合体/枚举类型声明，函数声明（static修饰）； 所有全局变量定义/函数实现；放在源文件。 **禁止**包含冗余的头文件。

---

请在实际编程中严格遵循这些规范，确保代码质量。
